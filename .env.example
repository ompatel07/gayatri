# Copy to .env and fill in. .env is gitignored - never commit real keys.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Optional DB overrides; config/db.php falls back to localhost/root/gayatri_db
# and then to the bundled SQLite file.
DB_HOST=
DB_USER=
DB_PASS=
DB_NAME=
